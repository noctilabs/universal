{
  "name": "universal-monorepo",
  "private": true,
  "packageManager": "yarn@4.12.0",
  "workspaces": [
    "packages/*",
    "backend",
    "storefront",
    "sanity"
  ],
  "scripts": {
    "build": "yarn build:types && yarn build:sanity:typegen && yarn build:sanity",
    "build:types": "yarn workspace @universal/types run build",
    "build:sanity": "yarn workspace @universal/sanity run build",
    "build:sanity:typegen": "yarn workspace universal-sanity run typegen && cp sanity/sanity.types.ts packages/sanity/src/sanity.types.ts"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "typescript": "^5.9.3"
  }
}
