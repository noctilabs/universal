# Landing assets

Replace these with exports from Figma when available:

- **Hero background**: Set `NEXT_PUBLIC_HERO_IMAGE_URL` in `.env.local` to your hero image URL, or place `hero-landing.jpg` in this folder and reference `/images/hero-landing.jpg`.
- **Logo**: Replace `logo.svg` with the design export (abstract geometric logo for the bottom nav bar).
